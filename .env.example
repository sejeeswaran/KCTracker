# KC Tracker Environment Variables Template
# Copy this file to .env and fill in the values

# Flask App Secret Key (Generate a random string for production)
SECRET_KEY=your_super_secret_flask_key_here

# Fernet Encryption Key (Generate using: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=your_generated_32_byte_base64_key_here
